package data

import (
	"time"

	"gorm.io/gorm"
)

// $MODEL_NAME struct
type $MODEL_NAME struct {
	ID        uint           `gorm:"primaryKey"`
	CreatedAt time.Time
	UpdatedAt time.Time
	DeletedAt gorm.DeletedAt `gorm:"index"`
}

// TableName returns the GORM table name for this model.
func (t *$MODEL_NAME) TableName() string {
	return "$TABLE_NAME"
}
