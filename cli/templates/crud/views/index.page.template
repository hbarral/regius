{{define "content"}}
<main class="flex-1">
  <div class="container mx-auto px-4 py-8">
    <div class="mb-6 flex items-center justify-between">
      <h1 class="text-3xl font-bold tracking-tight">$TITLEs</h1>
      <a href="/$TABLE/new" class="text-sm font-medium text-primary hover:underline">New $TITLE</a>
    </div>
    <!-- TODO: list the records passed as .Data.items -->
    <p class="text-muted-foreground">No $TABLE yet.</p>
  </div>
</main>
{{end}}
