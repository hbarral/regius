package resources

import "time"

// $RESOURCE_NAME shapes a $resource_name model for API responses (the data
// field of the api.Response envelope). Generated by `regius make resource`;
// edit freely.
type $RESOURCE_NAME struct {
	ID        int64     `json:"id"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
	// TODO: add fields from your $resource_name model
}

// New$RESOURCE_NAME maps a $resource_name model to its API resource.
func New$RESOURCE_NAME(m /* TODO: models.$MODEL_NAME */ any) *$RESOURCE_NAME {
	return &$RESOURCE_NAME{
		// TODO: map model fields, e.g. ID: m.ID,
	}
}

// New$RESOURCE_NAME_PLURAL maps a slice of $resource_name models.
func New$RESOURCE_NAME_PLURAL(items /* TODO: []models.$MODEL_NAME */ []any) []*$RESOURCE_NAME {
	out := make([]*$RESOURCE_NAME, 0, len(items))
	for _, item := range items {
		out = append(out, New$RESOURCE_NAME(item))
	}
	return out
}
